== README

Basic first web scraper project.

Scrapes names & prices of men's sneakers.

-----------------------------------------
Clone this repo.

In your Terminal/Command Prompt:

$ cd into the newly cloned directory.

gem install httparty

$ cd lib

$ ruby scraper.rb

end
